#pragma once

//#include <SDL_mixer.h>

class sounds {
/*
public:
	sounds() {
		// setup audio
		Mix_Init(0);
		Mix_OpenAudio(44100, MIX_DEFAULT_FORMAT, 2, 1024);

		catchSound = Mix_LoadWAV("./audio/pop.wav");
		buttonClick = Mix_LoadWAV("./audio/buttonClick1.wav");
	}

	static inline Mix_Chunk* catchSound;
	static inline Mix_Chunk* buttonClick;
*/
};